build/
install/
work/
__pycache__/
gvsoc_build_new/
*.o
*.pyc
*.so
power_report.csv
trace_file.txt
trace_*.log
gvsoc_trace.log
traces/
work*/
compile_commands.json
compile_flags.txt
.clangd

# Generated GVSOC DPI config artifacts (regenerated by `make config` before each sim)
gvsoc_config*.json

# Compiled standalone unit-test binary + transient trace artifacts (`make test`)
/test/test_rvvi_text_writer
/test/*.rvvi

# Compiler dependency files (-MMD) + gvrun runtime config dump
*.d
gvsoc_runtime_config.txt

# Agent session memory (local, not versioned)
.claude/